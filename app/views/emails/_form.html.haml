= semantic_form_for @email do |f|
  = f.inputs do
    = f.input :person, :include_blank => false, :label_method => :display_name, :collection => Person.order_by_last_name
    = f.input :name, :label => 'Email Type'
    = f.input :address
  = f.buttons do
    = f.commit_button

